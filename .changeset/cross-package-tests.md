---
---

Add cross-package unit tests: `keyToAction` and `useCalculator` / `useGlobalKeyboard` hook coverage (`@examind/calculator-react`) and a MUI skin interaction test (`@examind/calculator-mui`). Test-only, so no package version bump.
